meta {
  name: rejected
  type: http
  seq: 3
}

post {
  url: {{LIBREBREAD_URL}}/libre/payment/{{payment_id}}/status/rejected
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
